import React from 'react';

function SettingsButton() {
  // Implement functionality for Settings
  // (e.g., manage categories, user preferences)
  return (
    <button>Settings</button>
  );
}

export default SettingsButton;
